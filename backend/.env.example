# Caption Studio Backend - Environment Variables Configuration Template
# Place direct credential secrets inside .env at this directory level (DO NOT COMMIT THE ACTUAL .env FILE)

# Server Configuration Options
PORT=10000
FRONTEND_URL=https://your-frontend-vercel-domain.vercel.app

# Whisper API Provider Settings (Local model or Endpoint)
WHISPER_API_URL=https://api.openai.com/v1/audio/transcriptions
WHISPER_API_KEY=your_whisper_api_key_goes_here
WHISPER_MODEL=whisper-1

# Optional Advanced Control Settings
# Maximum duration to wait for transcription API responses (in milliseconds)
WHISPER_TIMEOUT=60000
# Maximum retry attempts for transient API drops
WHISPER_MAX_RETRIES=3
# Keep audio/video processing intermediate files for diagnostic debugging (true | false)
KEEP_TEMP_FILES=false

# Audio timeline (sound effects + imported audio tracks)
# Where the bundled sound-effect assets live on disk. Defaults to the SAME
# public/sounds/ directory the browser serves them from, so the live preview
# and the exported file mix the identical waveform.
# SOUNDS_DIR=/absolute/path/to/sounds
# Where user-imported audio tracks are stored. Defaults to backend/audio/.
# AUDIO_UPLOADS_DIR=/absolute/path/to/audio
# How long an imported audio track is kept before the cleanup daemon sweeps it.
# Much longer than CLEANUP_TIMEOUT_MS above: a music bed stays referenced by the
# editor's timeline for the whole session, unlike a per-job render intermediate.
# AUDIO_RETENTION_MS=86400000
